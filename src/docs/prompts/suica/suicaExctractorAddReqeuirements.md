# 1
経費申請のための物だから、チャージ履歴もいれてほしいかも。

# 2
抽出するデータは、その月の1日から末日までに限定してほしい。

# 3
ダウンロードしたPDFは、`{YYYYMM}_{チャージ時間}_{チャージ金額}円_suica.pdf`のような名前で保存してほしい。

# 4
絶対に、入出記録単体になることはない。
チャージ履歴とセットになるはず
月をまたいでチャージ履歴がある場合は、例外としてそのチャージ履歴も含めてほしい。

# 5
無視せずに、もしチャージ記録がない取引があったら、それはMarkdownとかで出力して